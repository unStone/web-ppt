---
title: 让官网完整支持中英文
status: open
assignee: /root
labels:
  - wayfinder:task
parent: ../map.md
blocked_by: []
---

## Question

官网首页、查看器和编辑页目前只有中文。如何提供完整中英文产品体验，同时保持发布包零文案、静态部署和按需
模板入口不变？

建立类型安全的站点词条目录与唯一语言解析规则，覆盖 HTML metadata、导航、上传/错误/恢复、工具栏、模板选择、
无障碍标签和动态状态；显式 URL 语言优先于已保存偏好，再回退浏览器语言。语言切换保留当前文件、编辑历史、
选区和页面位置，不重建编辑会话；未知语言稳定回退英文，缺词在构建时失败。静态中文/英文可被搜索引擎直接
发现，`hreflang`、canonical、JSON-LD 和 Open Graph 与页面语言一致。

验收覆盖两种语言的三张页面、键盘与触屏入口、模板延迟加载、错误与恢复分支、刷新记忆、深链接和无脚本元数据；
真实 Chrome 检查可见文本及可访问名称，构建守卫证明 core/edit-core/editor 不含站点词条。

## 阶段推进（保持 open）

| 基础增量 | 后续仍须完成 |
|---|---|
| 三张页面共用中文源文案与类型安全英文目录，构建输出同级 `*.en.html` 静态镜像 | 查看器和编辑器各条动态状态、错误、恢复与按需工具文案 |
| URL → 保存偏好 → 浏览器语言；未知语言回退英文，中文默认不加载英文目录 | 框架视图生成的无障碍名称与完整键盘/触屏语言矩阵 |
| 原地切换静态 UI、标题/文稿类型、撤销重做状态和元数据，不重建编辑会话；中文地址保留 | 全站双语验收，不能以静态页面翻译代替完成态 |

源文案作为词条身份；只认领页面骨架及显式消息绑定，不遍历或按词猜测翻译幻灯片、文件名、样本标题和署名。
参数以文本插值传递，静态构建对缺词失败；两个语言页面共享结构和脚本资源，避免维护两套会漂移的 HTML。

## 基础增量验证入口

| 检查 | 命令 / 合约 |
|---|---|
| 离线缺词、参数/重复译文冲突和发布包隔离 | `node tooling/check-site-i18n.mjs`（已接入 build / verify；source-only 接入功能测试） |
| 类型参数与未知词条拒绝 | `tooling/type-contracts/site-i18n.ts`，随 `npm run check` |
| 实际静态构建、CSS 依赖闭包、英文导航、无脚本 SEO | `npm run test:site:i18n`，需先构建发布包；CI 与 Pages 部署均执行 |
| 编辑会话/选区/历史保留，偏好与未知语言刷新 | 官网真实 Chrome DOM 合约，不直接访问编辑器私有状态 |
| 静态英文量词为空后切回中文 | 读取英文产物，再点击中文；不以开发态代替产物验收 |
| 三页 320px 语言入口 | Chrome 真实触点切中文、Enter 切英文；检查语言与文件入口不被裁切 |

生产构建已验证根路径和 `SITE_BASE=/web-ppt/` 子路径。共享 CSS 内联后仍可能被动态预加载引用，
只能在 Vite 写完清单后删除真正无引用的样式；生产浏览器回归锁住这一故障。

2026-09-06 基础增量四项门禁全绿：5,894 项断言、186 个快照、550 对独立进程 SVG 指纹、八包构建、
355 项一致性检查通过；原性能和体积预算保持不变。Standards / Spec 独立审查与复审均无未解决发现。
这只是基础增量的验收，不关闭完整国际化票据。
