---
title: 插入可交付的音视频
status: open
assignee: /root
labels:
  - wayfinder:task
parent: ../map.md
blocked_by: []
---

## Question

现有解析器能读音视频，编辑器却不能插入。如何新增音频、视频和海报图而不建立第二套媒体资源模型，并保证
保存、复制粘贴、恢复与协同后的文件仍能离线播放？

提供纯数据 `AddMedia` 命令：媒体可来自字节或显式外链，视频必须支持海报图，音频使用确定性内置图标或用户
海报；MIME/扩展名按签名字节与受控表确认，不信任文件名。嵌入资源按内容哈希去重并同时写入 `audio` / `video`
与 `p14:media` 兼容关系，外链永不冒充嵌入。命令复用图片插入的几何、关系闭包、历史和稳定身份，框架适配与
官网只转发同一 seam。

验收覆盖 MP4、WAV、外链、重复字节、替换海报、复制/删除、撤销重做、恢复/协同、补丁/生成保存、离线重开、
真实 Chrome 播放控件和 LibreOffice 无修复打开；恶意 MIME、超限字节与失效 URL 明确拒绝或降级，默认入口无
媒体编码器和示例字节。

## 阶段增量（票据保持 open）

| 已实现 | 尚未完成 |
|---|---|
| PCM WAV / MP4（含分片）与显式外链、确定性音频图标、自定义及替换海报 | Windows PowerPoint 实测 |
| 原子插入/选中、复制删除、撤销重做、注册恢复、并发协同；四个包按需入口复用同一接口 | `.ppt` 复制仍需先保存并重开 |
| 两种保存的双关系、原字节去重、严格 XML；无原包 `.ppt` 的插入与海报生命周期 | — |
| 官网按需媒体工具、仅媒体日志注册恢复、选中媒体原生播放与失效外链提示 | — |

官网交互回归覆盖 WAV 冷启动恢复与换海报、MP4 保存重开后的断网实际画面帧、有效/404 外链、无海报与
伪装 WAV 拒绝、慢响应下连续播放，以及拖放隔离、切换文稿清理和关闭工具后的 DOM 回收。
播放器属于主动打开的媒体工具，默认编辑画布仍展示海报，不引入第二套投影或模型。

API、明确的输入范围与验证命令见[媒体插入](../../../media-insertion.md)。此前已修复通用插入宿主离开临时
XML 祖先时丢失命名空间闭包的问题；严格 DOM 解析与 Chrome 播放作为独立回归，不能以宽松解析或
LibreOffice 能打开代替它们。

| 媒体 API 阶段实测 | 结果 |
|---|---|
| 全仓门禁 | check / test / build / verify 全绿；5894 项断言、186 个快照、550 对等价指纹、355 项一致性检查 |
| 发布入口 | 源码 870 项、构建后 867 项媒体契约通过（另 3 项为源码打包注册恢复）；默认入口 82499 B gzip，原预算不变 |
| Chrome | 14 种嵌入/外链、默认/替换海报与两种保存组合均真实点击并播放结束；WAV 0.4 秒、MP4 0.6 秒、fMP4 0.728027 秒，视频产生实际画面帧 |
| LibreOffice | 26 种保存产物均打开并导出预期页数 PDF；不是 PowerPoint 播放证据 |
| 固件 | 92 份固件连续生成两次逐字节相同；视频码流由仓库自制，固定字节保证跨编码器版本确定性 |
| 审查回归 | 无海报导入后保存、MC 原壳保护、无原包资源保留、协同撤销重做资源闭包、双向历史超限原子拒绝及合并超限保留独立单元均有回归 |
