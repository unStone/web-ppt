---
title: 支持经典图表的多级类别编辑
status: closed
assignee: codex
priority: P0
labels:
  - wayfinder:task
parent: ../map.md
blocked_by: []
---

## Question

如何在不压平层级、空槽和重复标签的前提下编辑 `multiLvlStrRef`，并同步图表缓存与内嵌工作簿？

## 范围与路径

- 从 `chart/source.ts` 的多级类别只读分支建立最小复现；区分层级路径、显示标签与单元格地址。
- 扩展稳定类别身份与绑定模型，提供查询、改名、增删类别及层级路径编辑；兼容既有平面 ChartDataset 调用。
- 原子写入层级缓存、公式范围和工作簿单元格；不支持的公式绑定保留只读。
- 官网数据面板保留层级与空槽；图表类型/样式编辑沿用同一可编辑性判断。

## 验收

- 两级/三级类别、重复标签、缺失层级、空字符串与数值零、多个系列及增删后的层级跨度。
- 保存前后缓存与工作簿一致；平面图表、组合图与只读边界不回归。
- 历史、恢复、协同和公开类型兼容；两条保存、真实浏览器及 LibreOffice 对照通过。
- 满足[共同完成条件](../plan.md#共同完成条件)。

## Answer

2026-09-08 完成。[支持矩阵、公开 API 与成本](../../../chart-hierarchical-categories.md)已记录。

- `levels` 保留根到叶的原生槽位；按单级字段编辑、撤销和合并，稳定身份不依赖标签。
- 同步原生多级缓存、矩形公式及纵向/横向工作簿；重复父标签、空字符串、缺失槽与数值零保持区别。
- 用删除记录及创建/清空时的前驱恢复组跨度，避免删组首、撤销或并发路径不完整时丢组或复活旧组。
- 官网中英文逐级编辑与空槽开关已接入；类型/样式共用只读判断。缓存不符、歧义公式和越界写入原子拒绝。

| 验收 | 实测结果 |
|---|---|
| 源码与包名 | `test:chart-hierarchy` / `test:chart-hierarchy:dist` 各 178 项；纳入 `test` / `verify`，原平面图表 258 项不回归 |
| 保存与渲染 | 补丁/生成、源包释放、冷恢复、稳定身份、历史与协同；两条文字路径共 12 对独立进程 SVG 逐字节一致 |
| 官网 | 源码完整浏览器门禁及 `test:site:i18n` 生产页面中英文工作流通过；包括逐级编辑、空槽、撤销/重做和保存重开 |
| 独立读取器 | LibreOffice 26.2.5.2 保留两级/三级结构、非空父标签、重复叶标签和数值，补丁/生成重存一致；显式空串被读取器省略的边界已公开 |
| 固件 | 110 份连续生成两次逐字节一致；新增三页样本 SHA-256 为 `2589de6e6a8df36b2bec030daf25d89d206995831ae455d6fdb3932b618ee2c0` |
| 全仓 | `check` → `test` → `build` → `verify` 全绿；8950 项断言、186 个快照、103 份固件/351 页/702 对编辑等价指纹、8 包构建 |
| 成本 | 图表按需入口 77,898B / 24,737B gzip，原预算不变；Node 24.3.0、25 次编辑保存，中位 16.1ms、p95 35.3ms；内存采样及限制见支持矩阵 |

| 复审维度 | 结果 |
|---|---|
| Standards | 已修复规模上限与删空系列保存边界，最终无阻断项 |
| Spec | 已修复显式空槽、组跨度撤销及不完整并发路径，最终无阻断项 |

原始日志位于 `out/chart-hierarchy/`。画布仅绘制叶级轴，父级保留在面板与原生文件中；经典图表跨稿复制仍要求匹配的 OPC 闭包。
Windows PowerPoint 真机验收按原决定暂缓。下一张为[共享工作簿同步](003-shared-chart-workbook.md)，本阶段地图尚未完成。
