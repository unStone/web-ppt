# Web-PPT 领域语言

本上下文定义演示文稿从只读解析到可编辑、可保存期间的核心概念，避免把文件格式、渲染投影与用户编辑状态混为一谈。

## Language

**演示文稿（Presentation）**:
从源文件解析出的只读展示模型；它表达当前可渲染结果，不承担用户编辑身份、历史或写回语义。
_Avoid_: 编辑文档、工程文件

**编辑文档（EditDoc）**:
一次编辑会话中的可变文档，保留源文件语义，并只记录用户明确做出的覆盖。
_Avoid_: Presentation、幻灯片 JSON

**源值（Source Value）**:
从文件和继承链求值得到、在编辑会话中保持只读的属性值。
_Avoid_: 默认值、原始 XML 值

**覆盖值（Override）**:
用户明确改过的字段；删除覆盖值表示恢复源文件的继承结果。
_Avoid_: 当前值、完整样式

**有效投影（Effective Projection）**:
源值与覆盖值合成后交给既有渲染器的只读幻灯片视图。
_Avoid_: 保存结果、EditDoc 副本

**补丁保存（Patch Save）**:
在原演示包上只写入用户覆盖、并保留未触碰内容的保存方式。
_Avoid_: 导出、重新生成

**生成保存（Generated Save）**:
没有可补丁原包时，从编辑文档构造新 `.pptx` 的保存方式。
_Avoid_: 补丁保存、格式转换

**查看模式（View Mode）**:
只允许浏览、搜索、播放与导出的产品状态，不产生编辑覆盖。
_Avoid_: 只读文件

**编辑模式（Edit Mode）**:
允许通过命令修改编辑文档、形成历史并保存的产品状态；它与查看模式共享同一有效投影和渲染路径。
_Avoid_: 设计模式、画布模式

**框架对象（Frame-editable Object）**:
内部内容不可安全改写，但允许改变位置、尺寸、层级或删除的演示对象。
_Avoid_: 不可编辑对象、普通元素
